<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.edu.uniritter.mobile.nossaprimeiraappnoite">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NossaPrimeiraAppNoite">
        <activity
            android:name=".PostActivity"
            android:label="@string/title_activity_post"
            android:theme="@style/Theme.NossaPrimeiraAppNoite.NoActionBar"></activity>
        <activity android:name=".DetalhePostActivity" />
        <activity android:name=".DetalheCommentActivity" />
        <activity android:name=".ActivityComments" />
        <activity android:name=".AlbumsActivity" />
        <activity android:name=".DetalheTodoActivity" />
        <activity android:name=".SplashActivity" />
        <activity android:name=".DetalheAlbumActivity" />
        <activity
            android:name=".SegundaActivity"
            android:parentActivityName=".SegundaActivity">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <activity android:name=".PrincipalActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>